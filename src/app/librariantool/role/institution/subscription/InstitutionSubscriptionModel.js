/**
 * Login Model
 * Model for root /index.html template that wraps every other view.
 */

angular.module('platform-ui.librariantool.role.institution.subscription').factory(
	/* Name */
	'InstitutionSubscriptionModel',

	/* Dependencies */
	[

	function () {
		return {
			title: 'SUBSCRIPTION',
		};
	}
]);
