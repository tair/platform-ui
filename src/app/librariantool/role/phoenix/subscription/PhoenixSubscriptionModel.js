/**
 * Login Model
 * Model for root /index.html template that wraps every other view.
 */

angular.module('platform-ui.librariantool.role.phoenix.subscription').factory(
	/* Name */
	'PhoenixSubscriptionModel',

	/* Dependencies */
	[

	function () {
		return {
			title: 'Phoenix',
		};
	}
]);
