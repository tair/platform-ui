/**
 * Login Model
 * Model for root /index.html template that wraps every other view.
 */

angular.module('platform-ui.librariantool.role.phoenix.usage').factory(
	/* Name */
	'PhoenixUsageModel',

	/* Dependencies */
	[

	function () {
		return {
			title: 'Phoenix',
		};
	}
]);
